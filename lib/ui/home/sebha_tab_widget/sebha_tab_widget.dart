import 'package:flutter/material.dart';

class SebhaTabWidget extends StatelessWidget {
  const SebhaTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Sebha'),
    );
  }
}