import 'package:flutter/material.dart';

class AhadethTabWidget extends StatelessWidget {
  const AhadethTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ahadeth'),
    );
  }
}